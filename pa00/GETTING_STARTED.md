## Getting Started

1. Install dependencies:  `npm install`
2. Run the development server in watch mode:  `npm run dev`
3. Open a browser to the indicated URL

To stop the development server, type CTRL-C in the terminal.
